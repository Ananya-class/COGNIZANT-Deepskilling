<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Define the BookRepository bean -->
    <bean id="bookRepository" class="com.library.repository.BookRepository" />

    <!-- Define the BookService bean and inject bookRepository -->
    <bean id="bookService" class="com.library.service.BookService">
        <!-- The 'name' attribute matches the property name (setter method) in BookService -->
        <!-- The 'ref' attribute matches the 'id' of the bean being injected -->
        <property name="bookRepository" ref="bookRepository" />
    </bean>

</beans>